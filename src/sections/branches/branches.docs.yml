sections_table:
  endpoints: >-
    protected-branch listing + classic branch protection + required-signatures sub-endpoint +
    app-by-slug actor lookup +
    GraphQL BranchProtectionRules + BranchProtectionRulesSnapshot + BranchProtectionRepository +
    BranchProtectionActorUser + BranchProtectionActorTeam + CreateBranchProtectionRule +
    UpdateBranchProtectionRule + DeleteBranchProtectionRule
  notes: >-
    `protection: null` removes protection; the protection PUT drops `required_signatures`, so
    declare it on any branch already carrying it; `force_push_bypassers` (users, `org/team`,
    `app/slug`) and `required_deployments` ride the GraphQL rule mutation; wildcard entries
    (`release/*`) reconcile entirely through GraphQL with a fixed key set; add Contents: read so
    check mode can tell a missing branch from an unprotected one
coverage:
  - area: >-
      [Classic branch protection](https://docs.github.com/en/rest/branches/branch-protection)
      (literal branches, wildcard patterns, force-push bypass actors, required deployments)
    notes: >-
      PUT /repos/{owner}/{repo}/branches/{branch}/protection passthrough; the four required keys are
      null-filled; protection: null issues DELETE (deleteBranchProtectionRule for a wildcard entry).
      Check mode flattens the GET shape ({enabled} wrappers, actor objects -> login/slug strings,
      *_url dropped) to compare like with like (src/sections/branches/index.ts flattenProtection).
      Every write is drift-gated: the PUT is planned when a declared key diverges or when the
      replacing PUT would remove a live setting the file omits, and a converged branch plans
      nothing. Three routed keys never ride the PUT: required_signatures (the PUT silently drops it)
      goes through its own POST/DELETE .../protection/required_signatures sub-endpoint when it
      drifts and again after any planned PUT (declared true POSTs, false DELETEs, undeclared
      untouched; a GET that omits the field reads as false; GitHub does not document whether the PUT
      preserves an existing requirement, so declare the toggle on any branch that carries one),
      while force_push_bypassers and required_deployments are REST-invisible entirely and ride ONE
      updateBranchProtectionRule GraphQL mutation on the same terms. force_push_bypassers is a list
      of actor strings - a bare login is a user, "org/team-slug" a team, "app/slug" a GitHub App -
      resolved to node ids when apply executes, ahead of the section's first write, so a misspelled
      actor fails before any write lands (check mode issues no lookup and reports the drift): users
      and teams through GraphQL (the BranchProtectionActorUser and BranchProtectionActorTeam
      lookups, new-format ids), Apps through the public GET /apps/{app_slug} REST lookup, whose
      node_id can still be the legacy format for old Apps (GitHub accepts it with a deprecation
      warning). required_deployments takes {environments: [...]}; declaring null turns the
      requirement off, an absent key leaves the live state untouched. CAVEAT (verified live): GitHub
      SILENTLY drops required-deployment environment names that do not exist while the mutation
      succeeds, so apply verifies the mutation's read-back and fails loudly naming any dropped name
      - the environments section runs before branches, so environments declared in the same file
      exist by then. WILDCARD entries (a name containing `*`, `?`, or `[`, e.g. release/*) are
      invisible to the REST endpoints (their docs point wildcard use at the GraphQL API), so they
      reconcile entirely through the
      createBranchProtectionRule/updateBranchProtectionRule/deleteBranchProtectionRule mutations (a
      create addresses the repository node id the BranchProtectionRepository query fetches) against
      the Repository.branchProtectionRules read; a wildcard protection accepts exactly the keys this
      section can round-trip through the GraphQL rule surface - enforce_admins (its isAdminEnforced
      twin live-verified bidirectionally against the REST view of the same rule),
      required_linear_history, allow_force_pushes, allow_deletions, block_creations,
      required_conversation_resolution, lock_branch, allow_fork_syncing, required_signatures,
      required_status_checks (strict, contexts), required_pull_request_reviews
      (required_approving_review_count, require_code_owner_reviews, dismiss_stale_reviews,
      require_last_push_approval), force_push_bypassers, required_deployments - and rejects anything
      else upfront (the actor-list controls have GraphQL fields but their REST vocabulary of
      database ids does not round-trip through node-id-based reads, so this section does not manage
      them on wildcard rules), pointing at rulesets as the recommended path for new configuration.
      The rules query fires only when an entry has a wildcard name or declares a GraphQL-routed key
      (a pure-REST declaration issues no GraphQL request), which also scopes the undeclared-rule
      NOTE: only a run whose declaration fires the query reports a live wildcard rule the file does
      not declare, as a note and never a deletion; wildcard updates have PATCH semantics (an omitted
      key keeps its live value, unlike the literal PUT's replace). Actor and environment names
      compare case-insensitively (GitHub canonicalizes them), and the routed lists reject duplicate
      names upfront.
      Snapshot lists the protected branches (GET /repos/{owner}/{repo}/branches?protected=true)
      and reads each one's classic protection back in the PUT vocabulary, controls that are off
      omitted and the required checks in both spellings the PUT takes, `contexts` and `checks` (an
      any-App check as app_id -1, the PUT's spelling of GitHub's null read-back, which the diff
      normalizes the same way); a listed branch
      whose protection read answers 404 (a ruleset protects it) is left out. The snapshot's own
      rules read (BranchProtectionRulesSnapshot, the planner's selection under a declaration that
      tolerates no denial, so a concealed 404 is a PermissionDenied naming the grant, which the
      engine turns into a skipped section under on-missing-permission: warn and a failed run under
      fail, instead of reading as no rules) supplies the GraphQL-only surface: a literal entry carries its
      force_push_bypassers (actor strings in the file's spelling, sorted) and required_deployments
      when set, an empty allowance list and an off requirement omitted since an absent routed key
      leaves the live value untouched; every wildcard rule is written as its own entry with the
      off controls dropped; a branch only a wildcard rule protects is NOT written as a literal
      entry (the REST read serves that rule's protection under the branch's name, and a literal
      entry would create a second rule on apply); a literal-pattern rule whose branch the REST
      read did not surface (no such branch, or a denied probe) is a note, since a literal entry
      applies through the PUT that needs the branch.
schema:
  SettingsFile.branches: Classic branch protection per branch.
  BranchConfig: Classic protection for one branch name or wildcard pattern.
  BranchConfig.name: >-
    The branch name, or a wildcard pattern (any name containing `*`, `?`, or `[`, e.g. "release/*").
    A literal name applies through the REST protection endpoints; a wildcard rule is REST-invisible,
    so it applies entirely through the GraphQL branch-protection-rule mutations and its protection
    accepts only the keys this action can round-trip through that surface (the validator names them;
    prefer rulesets for new pattern-based configuration).
  BranchConfig.protection: PUT .../protection payload; null removes protection (Probot parity).
  BranchProtectionConfig: The protection PUT payload, passed through verbatim except its routed keys.
  BranchProtectionConfig.required_signatures: >-
    Require signed commits on the branch. On a literal branch this is a routed key the protection
    PUT silently drops, so it is applied through the POST/DELETE .../protection/required_signatures
    sub-endpoint: when it drifts, and again after any protection PUT (GitHub does not document
    whether the PUT preserves an existing requirement), so declare the toggle on any branch that
    carries one - a declared value is pinned either way. On a wildcard rule it rides the GraphQL
    rule mutation like every other key.
  BranchProtectionConfig.force_push_bypassers: >-
    Who may force-push to the branch when "allow force pushes" is in its "specify who" mode. Each
    actor is one string: a bare login is a user ("octocat"), "org/team-slug" is a team, and
    "app/slug" is a GitHub App. A REST-invisible surface: on a literal branch this routed key is
    stripped from the protection PUT and applied through the updateBranchProtectionRule GraphQL
    mutation when it drifts, and again after any protection PUT; on a wildcard rule it rides the
    create or update mutation with the rest of the rule. The live list is read back through GraphQL.
    An empty list clears every allowance; an absent key leaves the live list untouched.
  BranchProtectionConfig.required_deployments: >-
    Require deployments to succeed before merging (the checkbox and its environment list).
    REST-invisible like force_push_bypassers, so the routed key rides the same GraphQL mutation.
    Declaring `null` turns the requirement OFF; an absent key leaves the live state untouched.
    GitHub SILENTLY drops environment names that do not exist on the repository, so apply verifies
    the mutation's read-back and fails loudly naming any dropped name; the environments section runs
    before branches, so environments declared in the same settings file exist by the time this key
    applies.
  BranchProtectionConfig.required_deployments|0.environments: The environments whose deployments must succeed before merging.

name: codespaces-secrets-snapshot-roundtrip
description: >-
  mode: snapshot reads the live codespaces_secrets back into snapshot.yml and writes nothing to
  GitHub. The codespaces_secrets names read back as per-store $SECRET_CODESPACES_<name> references,
  one note each in the file header and as a notice; the values, which GitHub never reveals, come
  from the environment when the snapshot is checked back. The written file, fed back to a check run
  against the same state, reads clean.
settings: {}
live_state:
  codespaces_secrets:
    - name: DEPLOY_TOKEN
      created_at: 2026-01-01T00:00:00Z
      updated_at: 2026-01-01T00:00:00Z
    - name: GITHUB_PAT
      created_at: 2026-01-01T00:00:00Z
      updated_at: 2026-01-01T00:00:00Z
env:
  SECRET_CODESPACES_DEPLOY_TOKEN: deploy-token-plaintext-7f3a
  SECRET_CODESPACES_GITHUB_PAT: pat-plaintext-91c2
inputs:
  mode: snapshot
  snapshot_file: snapshot.yml
  sections: codespaces_secrets
expect:
  exit_code: 0
  result: snapshot
  outcomes:
    codespaces_secrets: snapshot
  snapshot:
    codespaces_secrets:
      _undeclared: keep
      entries:
        - name: DEPLOY_TOKEN
          value: $SECRET_CODESPACES_DEPLOY_TOKEN
        - name: GITHUB_PAT
          value: $SECRET_CODESPACES_GITHUB_PAT
  never:
    - POST /
    - PATCH /
    - PUT /
    - DELETE /
  snapshot_converges: true

name: dependabot-secrets-snapshot-roundtrip
description: >-
  mode: snapshot reads the live dependabot_secrets back into snapshot.yml and writes nothing to
  GitHub. The dependabot_secrets names read back as per-store $SECRET_DEPENDABOT_<name> references,
  one note each in the file header and as a notice; the values, which GitHub never reveals, come
  from the environment when the snapshot is checked back. The written file, fed back to a check run
  against the same state, reads clean.
settings: {}
live_state:
  dependabot_secrets:
    - name: DEPLOY_TOKEN
      created_at: 2026-01-01T00:00:00Z
      updated_at: 2026-01-01T00:00:00Z
    - name: RELEASE_PAT
      created_at: 2026-01-01T00:00:00Z
      updated_at: 2026-01-01T00:00:00Z
env:
  SECRET_DEPENDABOT_DEPLOY_TOKEN: deploy-token-plaintext-7f3a
  SECRET_DEPENDABOT_RELEASE_PAT: pat-plaintext-91c2
inputs:
  mode: snapshot
  snapshot_file: snapshot.yml
  sections: dependabot_secrets
expect:
  exit_code: 0
  result: snapshot
  outcomes:
    dependabot_secrets: snapshot
  snapshot:
    dependabot_secrets:
      _undeclared: keep
      entries:
        - name: DEPLOY_TOKEN
          value: $SECRET_DEPENDABOT_DEPLOY_TOKEN
        - name: RELEASE_PAT
          value: $SECRET_DEPENDABOT_RELEASE_PAT
  never:
    - POST /
    - PATCH /
    - PUT /
    - DELETE /
  snapshot_converges: true
